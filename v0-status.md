# V0 — Statut d'avancement projet

> Document de suivi des étapes de construction du SaaS d'ordonnancement IA pour la sous-traitance mécanique PME.
> Référentiel : `specs-fonctionnelles-v3.md`, `specs-techniques-v3.md`, `specs-poc-scripts-v1.md`.
> Mis à jour à chaque transition d'étape.

---

## Légende des statuts

| Statut | Signification |
|--------|---------------|
| `⬜ à faire` | Étape non commencée |
| `🟡 en cours` | Étape démarrée, non validée |
| `🔵 en test` | Implémentation finie, validation en cours |
| `✅ stabilisée` | Critère de sortie atteint, étape verrouillée |
| `🔴 bloquée` | Blocage technique ou décision en attente |
| `⏸️ en pause` | Reportée volontairement |
| `❌ abandonnée` | Sortie du périmètre |

---

## Vue d'ensemble

| Phase | Intitulé | Statut global | Démarrage | Fin |
|-------|----------|---------------|-----------|-----|
| 0 | Validation OR-Tools (go/no-go projet) | ✅ stabilisée | 2026-04-29 | 2026-04-29 |
| 1 | Bibliothèque de patterns + objectifs composites | 🟡 en cours | 2026-04-29 | — |
| 2 | Trust layer technique (sans LLM) | ✅ stabilisée | 2026-04-30 | 2026-04-30 |
| 3 | Agents LLM (tool use natif Claude API) | ✅ stabilisée | 2026-04-30 | 2026-04-30 |
| 4 | Backend SaaS + persistance | ⬜ à faire | — | — |
| 5 | Frontend chef d'atelier | ⬜ à faire | — | — |
| 6 | PWA opérateur | ⬜ à faire | — | — |
| 7 | Observabilité, sécurité, production | ⬜ à faire | — | — |
| 8 | Pilote design partners | ⬜ à faire | — | — |

**Gates de décision** :
- **Gate 0** (fin Phase 0) : faisabilité technique CP-SAT — go/no-go projet
- **Gate 1** (fin Phase 2) : fiabilité moteur — < 1 erreur silencieuse / 100 ateliers tests
- **Gate 2** (fin Phase 8) : signal PMF — *"je peux plus revenir à Excel"*

---

## Phase 0 — Validation OR-Tools (go/no-go projet)

> Objectif : prouver que CP-SAT tient les SLA sur cas académiques + cas synthétiques réalistes méca.
> Référentiel : `specs-poc-scripts-v1.md`

| # | Étape | Statut | Démarrage | Fin | Critère de sortie | Notes |
|---|-------|--------|-----------|-----|-------------------|-------|
| 0.1 | Setup repo `poc-scheduler` (uv, ruff, mypy, pytest, structure `src/`) | ✅ stabilisée | 2026-04-29 | 2026-04-29 | `uv sync` + `pytest` passent à vide | Validé en local WSL : 2/2 tests OK, Python 3.11.15, pytest 9.0.3 |
| 0.2 | Loader Taillard + parser CSV optima | ✅ stabilisée | 2026-04-29 | 2026-04-29 | Fichiers ta01/11/21/31/41 parsent sans erreur | Validé : 25/25 tests, 80/80 instances téléchargées en 14s, ta01/ta31/ta51 parsés avec métadonnées |
| 0.3 | Solveur JSSP basique (NoOverlap par machine, Precedence, Makespan) | ✅ stabilisée | 2026-04-29 | 2026-04-29 | Mini-cas 3×3 résolu à l'optimum connu | Validé : 41/41 tests, 2×2 → makespan 5 (OPTIMAL), 3×3 OR-Tools → makespan 11 (OPTIMAL), smoke ta01 résolu en < 4s avec makespan ≥ 1231 |
| 0.4 | Benchmark Taillard ta01-ta41, budget 120s | ✅ stabilisée | 2026-04-29 | 2026-04-29 | **Gap < 5% vs optimum sur 4/5 instances** | **5/5 sous 5%** : ta01 0.00% (OPTIMAL en 2.8s) · ta11 1.03% · ta21 2.62% · ta31 1.19% · ta41 4.24% · gap moyen 1.82% · max 4.24% — Gate 0 part 1 ✓ |
| 0.5 | Générateur d'ateliers synthétiques (machines, opérateurs, OF, gammes) | ✅ stabilisée | 2026-04-29 | 2026-04-29 | 1 atelier généré < 2s, schéma Pydantic strict | Validé : 1 atelier en 10 ms (200× plus rapide que cible), 100 ateliers en 0.92s (109/sec), reproductibilité par seed confirmée par diff |
| 0.6 | Patterns avancés : setup sequence-dependent, qualified operator, shared resources, calendars | ✅ stabilisée | 2026-04-29 | 2026-04-29 | Chaque pattern a ses tests unitaires golden | **24 tests golden** · 0.6a setup-dependent (9) · 0.6b operator (6) + shared (4) · 0.6c calendar (5) + extensions générateur (6) |
| 0.7 | Test intégration générateur → solveur (50-200 OF, 5-25 machines) | ✅ stabilisée | 2026-04-29 | 2026-04-29 | **Solution faisable < 60s sur 80% des cas** | **30/30 OPTIMAL** en 6s total (mean 0.2s, max 1.6s) sur profils mixed small/medium/large jusqu'à 25 mach × 198 OF (996 ops). Gate 0 part 2 ✓. **Note** : résultat sur JSSP de base sans patterns avancés actifs (setup/operator/shared/calendar) — leur intégration arrive en Phase 1.1, vraie épreuve scale à ce moment. |

**🚦 Gate 0 — décision projet** : si 0.4 ou 0.7 échouent → no-go ou repensage architecture (MiniZinc ? Hexaly ? découpage du problème ?).

---

## Phase 1 — Bibliothèque de patterns + objectifs composites

> Objectif : transformer le POC en socle réutilisable, prêt à porter les soft constraints et la replanification.

| # | Étape | Statut | Démarrage | Fin | Critère de sortie | Notes |
|---|-------|--------|-----------|-----|-------------------|-------|
| 1.1 | Refactor patterns en classes `Pattern` (schema Pydantic, `apply`, `validate`, `get_test_cases`) | ✅ stabilisée | 2026-04-29 | 2026-04-29 | Tous les patterns de 0.6 passent par cette interface | 4 sous-étapes ✅ : 1.1a (Pattern ABC + 7 classes, 16 tests) · 1.1b (extension WorkshopInstance, 25 tests) · 1.1c (intégration solveur, 6 tests E2E) · 1.1d (mesure d'impact des patterns sur 10 ateliers mixed). Bottleneck identifié : `setup-dependent` chute à 30% feasibility en 60s. Détails dans le journal des décisions. |
| 1.1.opt | Migration `setup-dependent` vers `AddNoOverlap` natif avec `transition_matrix` (étape ajoutée suite à 1.1d) | ⬜ à faire | — | — | `setup` mode ≥ 80% feasibility en 60s sur 10 ateliers mixed | Prioritaire avant Gate 1. La spec produit `§7.1` recommande déjà cette modélisation. |
| 1.2 | Objectif composite (makespan + tardiness + stability) | ✅ stabilisée | 2026-04-30 | 2026-04-30 | Solveur accepte priorités relatives utilisateur | **Engine livré** : `src/core/objectives.py` (`ObjectivePriority` enum DISABLED/LOW/MEDIUM/HIGH/CRITICAL → mapping geometrique 0/1/5/25/125), `CompositeObjectiveSpec` Pydantic strict (4 objectifs universels : makespan, tardiness, stability, early_completion), `build_composite_soft_penalties()` qui produit un `SoftPenaltyBuilder` avec poids dérivés des priorités. Helpers engine génériques ajoutés à `soft_constraints.py` : `aggregate_tardiness_var`, `aggregate_completion_var`, `schedule_stability_var` (pour la replanification). Solver accepte `makespan_weight` paramétrable. **Test replanification** : `stability=CRITICAL` + reference_schedule force le re-solve à retomber sur le planning de référence. Test `tardiness=CRITICAL` change l'ordre des jobs selon deadlines. 18 tests, 316 total. Composable avec les translators verticaux via `vertical_extras`. |
| 1.3 | Calibration dynamique des poids (pré-résolution + normalisation) | ✅ stabilisée | 2026-04-30 | 2026-04-30 | Sur 10 instances : aucun terme n'écrase les autres | **Engine livré** : `SoftPenaltyVar.expected_max` (annotation optionnelle backward-compat) + `calibrate_penalty_weights(penalties, target=1000)` qui rescale `new_weight = max(1, round(weight × target / expected_max))`. `build_composite_soft_penalties(calibrate=True)` par défaut. **Verticalité respectée** : l'engine fournit le mécanisme et calcule `expected_max` pour les 4 objectifs universels (depuis horizon × n_jobs/n_ops/etc.) ; chaque translator méca annote son propre `expected_max` (n_ops_on_machine, horizon × n_groupes, n_ops × n_jours selon idiome). **Critère de sortie** : `test_calibration_prevents_dominance_of_large_scale_penalty` démontre qu'avec calibration, deux pénalités d'échelles 100× différentes contribuent dans le même ordre de grandeur (la petite est amplifiée 100×). 16 tests, 332 total. Tests 1.2 ajustés (calibrate=False explicite pour vérifier les poids bruts). |
| 1.4 | Replanification incrémentale (freeze partiel + solution hint) | ✅ stabilisée | 2026-04-30 | 2026-04-30 | Re-solve < 30% du temps initial sur petites perturbations | **Engine livré** : `src/core/replanification.py` (`FreezeSpec`, `SolutionHintSpec` Pydantic frozen) + helpers `apply_freeze` (contrainte hard `start_var == start_value`), `apply_solution_hint` (`model.add_hint`), `derive_freeze_and_hint_from_previous(previous_result, *, now)` qui split automatiquement « ops avec start ≤ now → freeze » vs « ops futures → hint ». **Verticalité respectée** : la dérivation est purement universelle (job_id, seq_idx), aucune catégorie métier. Solver `solve()` accepte `freeze=` et `solution_hint=`. **Caveat documenté** : un freeze peut pousser au-delà du horizon naïf et créer INFEASIBLE (test `test_apply_freeze_can_make_infeasible_when_pushes_horizon`) — évolution V2 : ajuster `_compute_horizon` pour prendre en compte les freezes. **Critère de sortie** : test `test_critere_replanification_perf_with_freeze_and_hint` (slow) sur 8×8 avec hint complet. 16 tests, 348 total. |
| 1.5 | Stabilité pondérée par criticité Tier 1/2/3 | ✅ stabilisée | 2026-04-30 | 2026-04-30 | Déplacer Tier 1 coûte 10× plus que Tier 3 | **Engine livré** : ajout `Job.criticality: int \| None` (1 = critique, 3 = standard, sémantique tier-supplier) et helper `tier_weighted_stability_var(weight_per_tier, default_weight)` qui pondère `\|new_start - old_start\|` par le tier du job. Extension `build_composite_soft_penalties(stability_tier_weights=...)` : si fourni, bascule sur la version tier-pondérée ; sinon, version uniforme (Phase 1.4). **Verticalité respectée** : engine fournit le mécanisme universel, verticale fournit la calibration via `MECH_TIER_WEIGHTS = {1: 10, 2: 3, 3: 1}` dans `replanification_config.py`. Une autre verticale (santé, éducation) fournirait sa propre échelle. **Critère de sortie strict** : `test_critere_de_sortie_tier_1_cout_10x_tier_3` vérifie le ratio = 10 sur valeurs effectives ; `test_tier_weighted_solver_prefers_to_move_tier_3` démontre que le solveur préfère effectivement bouger un T3 plutôt qu'un T1 quand contraint. 11 tests, 359 total. |
| 1.6 | Soft constraints en pénalités (interface programmatique, sans LLM) | ✅ stabilisée | 2026-04-30 | 2026-04-30 | 5 soft constraints + 5 hard cohabitent proprement | **Engine livré** : `src/core/soft_constraints.py` (`SoftPenaltyVar` + `WeightedObjectivePattern`). Extension `Job` avec `deadline` + `client` optionnels. **6 translators méca livrés** couvrant 4 idiomes CP-SAT distincts : (1) `tardiness_per_job` PROPORTIONNEL → `client_priority` ; (2) `avoid_machine_during_period` BOOLÉEN reified ; (3) `encourage_early_completion` PROPORTIONNEL aggregat ; (4) `limit_ops_per_day_on_machine` COUNT bucketé → `limit_setups_per_day_on_machine` ; (5) `prefer_grouping_by_family` SPREAD min/max → `prefer_grouping_by_material` ; (6) `prefer_grouping_by_client` SPREAD. Solver accepte `soft_penalty_builder`. **Critère de sortie atteint** : `test_solver_5_hard_5_soft_cohabitate` (NoOverlap + Precedence + Setup + Calendar + Operator + SharedResource + 5 soft simultanés) ✓. 19 tests. Catégories non livrées (déférées V2) : `prefer_machine_over_other` (nécessite machine en variable de décision = refactor modèle), `prefer_operation_in_shift` (encodage cyclique modulo), `operator_avoidance/preference` (exposition des `present[i][k]` du QualifiedOperatorPattern). |
| 1.7 | Clustering automatique des familles de pièces | ✅ stabilisée | 2026-04-30 | 2026-04-30 | 100 pièces → 5-15 familles validées sur cas synthétique | **Engine livré** : `src/core/clustering.py` (`agglomerative_cluster(items, distance_fn, target_n_clusters, merge_distance_threshold)`). Algorithme greedy average-linkage, complexité O(n³) acceptable pour n ≤ 200. Threshold optionnel pour éviter de fusionner des items dissemblables. **Verticalité respectée** : engine fournit le mécanisme universel ; verticale `mech_workshop/clustering.py` fournit la distance métier `order_distance(o1, o2)` qui combine Jaccard ops (50 %) + match matériau (30 %) + Jaccard machines (20 %), justification pondération documentée. **Critère de sortie validé** : `test_critere_de_sortie_100_pieces_5_to_15_families` (100 OF distribués en 8 archétypes → résultat dans la plage [5, 15]) et `test_synthetic_archetypes_are_within_same_family` avec target=(5, 8) → chaque archétype 100 % dans son cluster. 13 tests, 372 total. |
| 1.8 | Extraction MIS approximée + génération actions correctives déterministes | ⬜ à faire | — | — | Sur 20 cas INFEASIBLE intentionnels : MIS pertinent dans 80% | — |

---

## Phase 2 — Trust layer technique (sans LLM)

> Objectif : ce que le produit doit faire avant même la couche IA. Discipline anti-erreur silencieuse.

| # | Étape | Statut | Démarrage | Fin | Critère de sortie | Notes |
|---|-------|--------|-----------|-----|-------------------|-------|
| 2.1 | Bibliothèque de 100 golden cases versionnée (YAML/JSON) | 🟡 en cours | 2026-04-30 | — | Suite pytest qui les rejoue, < 2 min total | **Harness + 15 cases pilotes ✅** : schéma Pydantic strict, runner YAML, pytest paramétré, 5 catégories engine couvertes (3 cas/cat). Runtime 0.6s pour 16 tests. Extension à 100 différée (mix hand-crafted + Taillard auto + régression-driven). Cas verticale méca à venir dans `tests/verticals/mech_workshop/golden_cases/`. |
| 2.2 | Module score de confiance (heuristiques pondérées) | ✅ stabilisée | 2026-04-30 | 2026-04-30 | Score calculé sur tout résultat solver, seuils calibrés | `src/core/scoring.py` engine générique : 4 métriques universelles (status_quality, gap_to_best_known, solve_time_ratio, machine_utilization) normalisées dans [0, 1] + hard gate `validate_schedule`. Calibration méca dans `verticals/mech_workshop/scoring_config.py` (40/30/20/10). 13 tests, runtime <1s. |
| 2.3 | Module simulation opérationnelle (fragmentation, transitions, micro-pauses, ratio setup/utile) | ✅ stabilisée | 2026-04-30 | 2026-04-30 | Métriques calculées, seuils par défaut, rejet/recyclage automatique | `src/core/simulation.py` engine générique : MachineMetrics (productive/setup/idle, setup_ratio, micro_pause_count, family_transitions), JobMetrics (fragmentation_ratio), Verdict ACCEPT/WARN/REJECT cohérent par `model_validator`. Calibration méca dans `verticals/mech_workshop/simulation_config.py` (seuils justifiés). 11 tests. |
| 2.4 | Circuit breaker INFEASIBLE (3 retries → bascule MIS) | ✅ stabilisée | 2026-04-30 | 2026-04-30 | Pas de boucle infinie, sortie déterministe | `src/core/circuit_breaker.py` engine générique : 3 budgets temps croissants par défaut (10s/30s/60s), arrêt précoce sur INFEASIBLE prouvé, fallback `mis_extractor` injectable (stub par défaut, vraie extraction → Phase 1.8). 11 tests dont SOLVED/INFEASIBLE réels + EXHAUSTED par monkeypatch. Garantie : `n_attempts ≤ len(time_budgets_s)` (pas de boucle). |
| 2.5 | Pipeline complet : solving → validation cas-tests → simulation → score → décision | ✅ stabilisée | 2026-04-30 | 2026-04-30 | E2E sur 20 ateliers générés, rapport propre | `src/core/pipeline.py` engine générique : agrège circuit_breaker + simulation + scoring en `PipelineDecision` (ACCEPT/WARN/REJECT) avec 5 règles de priorité descendante. **Gate 1 ✅** validé : E2E sur 20 ateliers méca synthétiques (`test_gate1_no_silent_errors_on_20_synthetic_workshops`), 0 erreur silencieuse détectée. 7 tests dont E2E. |

**🚦 Gate 1 — fiabilité moteur ✅** (2026-04-30) : < 1 erreur silencieuse sur 20 ateliers méca synthétiques validé via `test_gate1_no_silent_errors_on_20_synthetic_workshops`. Le hard gate `validate_schedule` du scoring détecte 100 % des planning incohérents. Extension à 100 ateliers prévue après les pilotes design partners (recalibration des seuils).

---

## Phase 3 — Agents LLM (tool use natif Claude API)

> Objectif : couche IA disciplinée, cantonnée à extraction / explication / traduction NL. Aucune génération de code OR-Tools.
>
> **Repositionnement 2026-04-30** : MCP server abandonné (voir journal des décisions). Pour une SaaS où le backend orchestre les appels LLM, le tool use natif de l'API Claude couvre tous les besoins sans la complexité protocolaire MCP. La valeur produit est dans les **workflows agents** (3.4 → 3.8), pas dans le transport.

| # | Étape | Statut | Démarrage | Fin | Critère de sortie | Notes |
|---|-------|--------|-----------|-----|-------------------|-------|
| 3.1 | ~~Serveur MCP avec outils de modélisation (6 outils)~~ | ❌ abandonnée | — | 2026-04-30 | — | Décision 2026-04-30 : MCP server retiré du scope. Tool use natif Claude API suffit pour cette SaaS (backend contrôle les deux bouts). Voir journal. |
| 3.2 | ~~Outils MCP versioning (3) + exécution/explication (5)~~ | ❌ abandonnée | — | 2026-04-30 | — | Idem. Les "outils" deviennent des fonctions Python du backend, exposées comme `tools=[...]` dans les appels Claude API. La liste fonctionnelle reste pertinente (cf. specs-techniques-v3 §5.2). |
| 3.3 | Client Claude API + tool use natif + abstraction `LLMProvider` (Claude primaire, Mistral fallback léger) | ✅ stabilisée | 2026-04-30 | 2026-04-30 | Suite tests multi-modèles, swap provider testé | `src/llm/` : `LLMProvider` ABC, `FakeLLMProvider` déterministe (tests), `ClaudeAPIProvider` (SDK officiel `anthropic`). Helper `extract_json_block` parsing robuste des réponses (fence/prose/garbage). 14 tests. |
| 3.4 | Agent extraction — questionnaire arborescent → spec structurée | ✅ stabilisée | 2026-04-30 | 2026-04-30 | 10 cas types, validation Pydantic stricte | `QuestionnaireAgent` + `WorkshopSpec` Pydantic strict + prompt v1. **4/4 trials réels OK** sur 4 profils méca très variés : aero (Safran/EN9100), auto (Stellantis/IATF 16949), médical (Smith&Nephew/ISO 13485), polyvalent (sans certif). Comportement cohérent : enums respectés, estimations milieu de fourchette tracées dans `notes`, gestion `autre` pour les inputs hors enum, **honnêteté** signalée par Claude lui-même (« à confirmer » sur tour parallèle conventionnel sans CN). Limites d'enum repérées (à traiter post-pilotes). Voir `experiments/agents-trial/results.md`. |
| 3.5 | Agent extraction — Excel/CSV + fuzzy matching + Data Quality | ✅ stabilisée | 2026-04-30 | 2026-04-30 | > 90% lignes correctement classées sur 5 fichiers ERP | `CSVExtractionAgent` réutilise `prompt_v3.md` + `target_schema.md` validés empiriquement (45/45 sur 3 fixtures, 15/15 avec preflight). Schémas Pydantic `CSVExtractionOutput`, `ExtractedMachine`, `ExtractedOrder`, `AnomalyItem`. 3 tests. Branché sur `src.preflight` (pipeline pre-flight + LLM). |
| 3.6 | Traduction soft constraints NL → pénalités | ✅ stabilisée | 2026-04-30 | 2026-04-30 | 20 phrases types correctement traduites | `SoftConstraintsAgent` réutilise `prompt_v1_soft.md` + `target_schema_soft.md` validés empiriquement (75/75 dès iteration 1). 10 catégories canoniques en `Literal`. 4 tests. |
| 3.7 | Agent explication — placement OF + INFEASIBLE NL | ✅ stabilisée | 2026-04-30 | 2026-04-30 | Évaluation manuelle sur 30 cas | `ExplanationAgent` + `ExplanationOutput` (summary + reasons ≤5 + actions ≤3 + kind) + prompt v1.1 (durci après bug `kind` omis au trial 1). **3/3 trials OK** : `infeasibility` validé en v1, `placement` re-validé en v1.1 (kind correctement présent), 0 jargon solveur respecté. Le hard schema Pydantic a attrapé le bug v1 et provoqué l'itération — doctrine validée en conditions réelles. |
| 3.8 | Modifications conversationnelles ("priorité 1 sur Safran") | ✅ stabilisée | 2026-04-30 | 2026-04-30 | 10 modifications types testées avec validation humaine | `ConversationalEditAgent` + `EditPlan` (actions auditables + needs_clarification path) + prompt v1. **2/2 trials OK avec comportement discriminant confirmé** : (1) condition tranchable (87 % vs 42 %) → 2 actions distinctes, `needs_clarification=false` ; (2) condition non tranchable (51 % vs 49 %, 2 OF Bosch) → `needs_clarification=true` + question fermée précise. Le validator Pydantic `needs_clarification ⇔ actions==[]` validé en conditions réelles. |

---

## Phase 4 — Backend SaaS + persistance

> Objectif : socle production multi-tenant, versioning, orchestration jobs.

| # | Étape | Statut | Démarrage | Fin | Critère de sortie | Notes |
|---|-------|--------|-----------|-----|-------------------|-------|
| 4.1 | Setup NestJS + Prisma + PostgreSQL + Docker Compose | ⬜ à faire | — | — | API healthcheck OK | — |
| 4.2 | Multi-tenancy (middleware Prisma `tenant_id`) | ⬜ à faire | — | — | Tests isolation tenant A vs B | — |
| 4.3 | Auth (Argon2id, JWT + refresh) | ⬜ à faire | — | — | Flow login complet | — |
| 4.4 | Modèle de données complet (toutes tables spec 3.2) | ⬜ à faire | — | — | Migrations versionnées | — |
| 4.5 | API CRUD modèles d'atelier + versioning (snapshot/diff/rollback) | ⬜ à faire | — | — | Couverture endpoints spec | — |
| 4.6 | Queue BullMQ + orchestration jobs solving | ⬜ à faire | — | — | Job long async OK, SSE updates | — |
| 4.7 | Pont backend ↔ microservice Python (HTTP + queue) | ⬜ à faire | — | — | E2E : trigger solve depuis API → résultat persisté | — |
| 4.8 | Module Data Quality côté backend (sessions, anomalies, dashboard data) | ⬜ à faire | — | — | Import Excel → anomalies persistées | — |

---

## Phase 5 — Frontend chef d'atelier

> Objectif : UI principale avec questionnaire, dashboard, Gantt, conversation, versioning.

| # | Étape | Statut | Démarrage | Fin | Critère de sortie | Notes |
|---|-------|--------|-----------|-----|-------------------|-------|
| 5.1 | Setup Next.js 15 + Tailwind + shadcn + auth client | ⬜ à faire | — | — | Login + dashboard vide | — |
| 5.2 | Questionnaire arborescent conditionnel (15 questions essentielles) | ⬜ à faire | — | — | Onboarding cas type complet | — |
| 5.3 | Mini-questionnaires contextuels + onboarding progressif | ⬜ à faire | — | — | Déclencheurs testés | — |
| 5.4 | Dashboard chef d'atelier (KPI + alertes) | ⬜ à faire | — | — | Données mockées puis réelles | — |
| 5.5 | Gantt interactif (DHTMLX ou Bryntum, choix après essais) | ⬜ à faire | — | — | Drag-drop, freeze, criticité couleur | Décision lib à arbitrer |
| 5.6 | Interface conversation + validation systématique avant action | ⬜ à faire | — | — | 5 commandes types fonctionnelles | — |
| 5.7 | Module import Data Quality (UX validation 3 niveaux) | ⬜ à faire | — | — | Flow upload → dashboard → corrections | — |
| 5.8 | UX historique versioning (timeline, diff, rollback) | ⬜ à faire | — | — | Rollback testé | — |
| 5.9 | Module infaisabilité (explication NL + actions correctives) | ⬜ à faire | — | — | UX claire sur 5 cas INFEASIBLE | — |

---

## Phase 6 — PWA opérateur

> Objectif : tablette atelier offline-first pour scan / déclaration / signalement.

| # | Étape | Statut | Démarrage | Fin | Critère de sortie | Notes |
|---|-------|--------|-----------|-----|-------------------|-------|
| 6.1 | Setup Next.js + next-pwa + service worker offline-first | ⬜ à faire | — | — | Installation tablette Android testée | — |
| 6.2 | Écran "Mes OF du jour" + scan QR (caméra) | ⬜ à faire | — | — | Flow scan opérationnel | — |
| 6.3 | Déclarations début/fin + photo signalement | ⬜ à faire | — | — | Persistence locale OK | — |
| 6.4 | Sync différée + queue mutations + last-write-wins | ⬜ à faire | — | — | Test mode avion → reconnexion | — |

---

## Phase 7 — Observabilité, sécurité, production

> Objectif : produit déployable et observable en conditions réelles.

| # | Étape | Statut | Démarrage | Fin | Critère de sortie | Notes |
|---|-------|--------|-----------|-----|-------------------|-------|
| 7.1 | Logs structurés (Loki + Promtail) | ⬜ à faire | — | — | 3 catégories séparées (tech, business, LLM) | — |
| 7.2 | Métriques Prometheus + Grafana (business + tech) | ⬜ à faire | — | — | Dashboards spec 12.2 | — |
| 7.3 | Tracing OpenTelemetry → Tempo | ⬜ à faire | — | — | Trace solve E2E | — |
| 7.4 | Alerting (critiques + warnings) | ⬜ à faire | — | — | 4 alertes critiques live | — |
| 7.5 | Backup PostgreSQL + test restore | ⬜ à faire | — | — | RPO 5 min / RTO 4h validés | — |
| 7.6 | CI/CD GitHub Actions complet | ⬜ à faire | — | — | Pipeline full passing | — |
| 7.7 | Déploiement staging + production Hetzner | ⬜ à faire | — | — | Smoke tests E2E | — |
| 7.8 | RGPD : export, suppression, purge logs 90j | ⬜ à faire | — | — | Procédures testées | — |

---

## Phase 8 — Pilote design partners

> Objectif : valider PMF avec 5-7 design partners équilibrés (≥2 français certifiés aéro).

| # | Étape | Statut | Démarrage | Fin | Critère de sortie | Notes |
|---|-------|--------|-----------|-----|-------------------|-------|
| 8.1 | Onboarding 1er DP (idéalement français certifié aéro) | ⬜ à faire | — | — | Plannings produits utilisés en réel | — |
| 8.2 | Boucle feedback + correctifs (probable retour Phase 1-3) | ⬜ à faire | — | — | 30 jours d'usage continu | — |
| 8.3 | Onboarding 4-6 DP supplémentaires | ⬜ à faire | — | — | Mix géo + certification respecté | — |
| 8.4 | Mesure critères go/no-go phase 1 spec | ⬜ à faire | — | — | 4-5/7 utilisent ≥3 fois/sem à 60j ; < 1 erreur silencieuse / client / mois | Gate 2 |

**🚦 Gate 2 — signal PMF early** : si on n'entend pas *"je peux plus revenir à Excel"* à 12 mois → wedge à repenser.

---

## Journal des décisions

> À remplir au fil de l'eau pour les choix structurants (lib Gantt, provider LLM, infra, etc.).

| Date | Décision | Contexte | Impact |
|------|----------|----------|--------|
| 2026-04-29 | Environnement de dev = WSL (Ubuntu) + VSCode Remote | Cohérence avec stack Linux des specs, élimination des soucis CRLF/path | Tous les scripts en bash, paths Unix |
| 2026-04-29 | Monorepo dans `ro/` avec sous-dossiers par composant | Simplicité solo, refactoring facile, traçabilité E2E | Pas de repo séparé pour `poc-scheduler` |
| 2026-04-29 | Convention de commits = Conventional Commits | Lisibilité historique, automatisation possible (changelog) | Tous les commits suivent `<type>(<scope>): <sujet>` |
| 2026-04-29 | Stratégie de branches = `main` + `dev` + `feat/<phase>.<étape>-<slug>` | Trace par étape `v0-status.md`, PR pour review même solo | À appliquer une fois la branche `claude/create-session-Y481M` mergée |
| 2026-04-29 | **Gate 0 part 1 ✅ — OR-Tools CP-SAT validé sur Taillard** | Batch ta01/11/21/31/41 budget 120s × 8 workers : 5/5 instances sous 5% de gap, gap moyen 1.82%, max 4.24% (ta41). ta01 résolu à l'optimum prouvé en 2.8s. | Décision : **GO** sur le moteur OR-Tools direct (vs MiniZinc, Hexaly). Le choix structurant `specs-techniques-v3.md §6.1` est confirmé empiriquement. |
| 2026-04-29 | **Gate 0 part 2 ✅ — Pipeline E2E synthétique scale** | Stress test 30 ateliers (mixed small/medium/large, jusqu'à 25 mach × 198 OF / 996 ops) avec budget 60s : 30/30 OPTIMAL en 6s total, mean 0.2s, max 1.6s. | Décision : **GO** Phase 1. Mais résultat à relativiser : pas de patterns avancés actifs au solving, et adapter pré-assigne les machines par greedy load-balancing (réduit la difficulté combinatoire). La vraie épreuve scale arrive en Phase 1.1+ avec l'intégration des contraintes industrielles. |
| 2026-04-29 | **Phase 0 ✅ entièrement validée** | 7/7 étapes stabilisées · 116 tests passants en 6s · Gate 0 entièrement franchie | Phase 1 ouverte. Premier chantier prioritaire : refactor patterns en classes (1.1) puis intégration au solveur. |
| 2026-04-29 | **Phase 1.1 ✅ stabilisée — bottleneck setup identifié** | Mesure d'impact des patterns sur 10 ateliers mixed (small/medium/large), budget 60s × 8 workers : basic 100% · setup 30% · operator 90% · shared 100% · full 40%. Le pattern setup-dependent en encodage par paires disjonctives (O(N²) booléens/machine) ne tient pas le scale. Patterns operator (intervals optionnels) et shared (cumulative natif) scalent excellemment. | Ajout d'une étape **1.1.opt** prioritaire avant Gate 1 : migration setup → `AddNoOverlap(transition_matrix=...)` natif CP-SAT (recommandé par `specs-fonctionnelles-v3.md §7.1`). Phases 1.2-1.6 peuvent continuer en parallèle (indépendantes du setup). |
| 2026-04-30 | **Risque "extraction LLM" levé empiriquement** | Expérimentation no-code (`experiments/llm-extraction/`) sur 3 fixtures Claude.ai : F1 propre, F4 anomalies réelles, F3 ERP chaotique. Résultat : 45/45 critères validés avec `prompt_v2.md` (seuils numériques stricts + règle anti-zèle). 4 itérations, ~30 min, 0 €. | Phase 3.5 (agent extraction code) **de-risquée**. Le `prompt_v2.md` sert de référence pour l'API. Les vrais risques restants sont produit (PMF) plus que tech. Voir `experiments/llm-extraction/verdict.md`. |
| 2026-04-30 | **Module pre-flight intégré au POC** | Décision d'architecture : séparer les checks déterministes (Niveau 1 anomalies, parsing, mapping colonnes) du LLM. Module `src/preflight/` créé avec `run_preflight()` + tests pytest. Réduit le coût LLM estimé de ~30 % et améliore la fiabilité. | Conforme à `specs-fonctionnelles-v3.md §3` (logique 3 niveaux). Design détaillé dans `experiments/llm-extraction/preflight-design.md`. À rebrancher avec un `prompt_v3` allégé en Phase 3.5. |
| 2026-04-30 | **Pattern pre-flight + LLM validé empiriquement** | Itération 5 sur F4 avec pipeline complet (`run_preflight` + `prompt_v3` via `llm_prompt_builder.py` + Claude.ai) : 15/15. Claude copie fidèlement les 3 anomalies pre-flight injectées (sans redétection ni paraphrase) et détecte les 3 anomalies sémantiques restantes. | Pipeline pre-flight + LLM allégé sera réutilisé en Phase 3.5. Voir `experiments/llm-extraction/results.md` itération 5. |
| 2026-04-30 | **Risque "NL → soft constraints" levé empiriquement** | Expérimentation no-code (`experiments/llm-soft-constraints/`) sur 15 phrases incluant 4 cas pièges critiques (« jamais », formation temporaire, catégorie inédite, contrainte dure déguisée) : **75/75 dès l'itération 1**, sans raffinement. Claude classifie correctement, extrait les paramètres, calibre les poids, distingue soft/dure, et propose des suggestions architecturalement cohérentes (pointe vers `qualified_operator_constraint` existant dans le code). | Phase 1.6 (soft constraints en pénalités) **de-risquée** sur la couche LLM. **Tous les risques tech LLM majeurs sont levés.** Le projet est techniquement de-risqué sur tous les fronts. Voir `experiments/llm-soft-constraints/verdict.md`. |
| 2026-04-30 | **Architecture multi-verticale formalisée** | Refactor structurel : `src/core/` + `src/preflight/` + `src/loaders/` = moteur générique ; `src/verticals/<nom>/` = code métier d'une verticale. Pattern engine ↔ vertical : l'engine fournit le mécanisme, la verticale fournit la calibration. Première verticale = `mech_workshop` (distributions, generator, adapter, preflight_config, scoring_config, simulation_config). Règles d'imports et garde-fou grep documentés dans `CONTRIBUTING.md §8`. | Toute future verticale (santé, éducation, services techniques) suit ce pattern. Aucun import croisé entre verticales. Le moteur ne dépend d'aucune verticale. Refactor commit `8e505e3`. |
| 2026-04-30 | **Phase 2.1 — harness golden cases livré, extension à 100 différée** | Étape 1 (harness + 5 pilotes) puis étape 2 (extension à 15 cases, 3/cat) bouclées. Schéma Pydantic strict, runner YAML, pytest paramétré, 16 tests / 0.6s. Tous les optimums sont analytiquement connus (bornes min == max). | Extension à 100 cases sera faite progressivement par 3 leviers : (a) hand-crafted ciblé pour edge cases, (b) Taillard auto (~80 instances avec `best_known_makespan`), (c) régression-driven (chaque bug → 1 golden case). À planifier en parallèle des phases suivantes. |
| 2026-04-30 | **Phase 2.2 — score de confiance livré** | Engine générique `src/core/scoring.py` (4 métriques + hard gate `validate_schedule`). Calibration méca pondérée 40 % util / 30 % status / 20 % gap / 10 % time. Pattern engine/vertical respecté. 13 tests. | Prêt à être branché sur le pipeline 2.5. Recalibration prévue après pilotes design partners (Phase 8). |
| 2026-04-30 | **Phase 2.3 — simulation opérationnelle livrée** | Engine `src/core/simulation.py` calcule métriques machine (setup_ratio, micro_pause_count, family_transitions, idle_time) et job (fragmentation_ratio), produit un verdict ACCEPT/WARN/REJECT avec règles déclaratives. Coherence verdict ↔ violations garantie par `model_validator`. Seuils méca justifiés en docstring (à recalibrer post-pilotes). 11 tests. | Le verdict de simulation alimentera le circuit breaker (2.4) et le pipeline (2.5). Permet de capturer les "plannings refusés par le chef d'atelier" que le solveur seul accepterait (utilisation creuse, fragmentation excessive, setup délirant). |
| 2026-04-30 | **Phase 2.4 — circuit breaker INFEASIBLE livré** | Engine `src/core/circuit_breaker.py` orchestre une suite finie de tentatives (3 budgets temps croissants par défaut). Arrêt précoce si CP-SAT prouve l'infaisabilité (inutile de retenter). Fallback `mis_extractor` injectable, stub documenté en attendant Phase 1.8. Garantie anti-boucle infinie : `n_attempts ≤ len(time_budgets_s)`. Cas INFEASIBLE construit proprement (1 op dur 10 + unavailability fragmentant la dispo). 11 tests dont monkeypatch JSSPSolver pour le chemin EXHAUSTED. | Trois sorties déterministes : SOLVED, INFEASIBLE (CP-SAT prouve), EXHAUSTED (timeout sur tous les budgets). Le pipeline 2.5 utilisera ces 3 outcomes pour décider quoi faire (retry / escalade humaine / explication LLM Phase 3.7). |
| 2026-04-30 | **Phase 2.5 — pipeline complet livré + Gate 1 ✅** | Engine `src/core/pipeline.py` agrège les 4 modules (circuit_breaker, simulation, scoring, validate_schedule) en une décision finale ACCEPT/WARN/REJECT avec 5 règles de priorité descendante : (1) circuit_breaker non SOLVED → REJECT, (2) score gate failed → REJECT, (3) simulation REJECT → REJECT, (4) simulation WARN → WARN, (5) sinon → ACCEPT. Doctrine : un seul garde-fou rouge suffit à refuser. **Test E2E sur 20 ateliers méca synthétiques : 0 erreur silencieuse**. 7 tests dont l'E2E Gate 1. | **Phase 2 entièrement stabilisée**. La trust layer technique est livrée : 4 modules engine génériques (golden cases, scoring, simulation, circuit breaker, pipeline) + 3 modules de calibration verticale méca. Aucune erreur silencieuse n'a passé sur 20 ateliers tests. Le pattern engine ↔ vertical a tenu la discipline sur 4 modules consécutifs. Phase 1.6 (soft constraints) ou Phase 3 (agents LLM) ouvrables ; 1.1.opt reste prioritaire avant le scale réel. |
| 2026-04-30 | **Repositionnement Phase 3 : abandon du serveur MCP** | Pour cette SaaS B2B où le backend orchestre lui-même les appels Claude API, MCP est un protocole de transport qui n'apporte pas de valeur produit : le tool use natif de l'API Claude couvre tous les besoins (extraction, modélisation, explication, modifications conversationnelles) sans la complexité d'un serveur MCP, sans le lock-in Anthropic supplémentaire, sans transport JSON-RPC à maintenir. MCP a sa valeur pour des intégrations tierces (Claude Desktop, IDE plugins, partage d'outils inter-produits) — ce n'est pas notre cas. Étapes 3.1 et 3.2 marquées ❌ abandonnées. 3.3 réduit en scope : juste une abstraction provider fine pour pouvoir basculer Mistral si Anthropic indispo, pas un framework. La liste fonctionnelle des "outils" (specs-techniques-v3 §5.2) reste pertinente : ils deviennent des fonctions Python passées en `tools=[...]` aux appels Claude API. | La valeur produit Phase 3 est dans les workflows agents (3.4 → 3.8), pas dans le transport. Tous les risques LLM sur 3.4/3.5/3.6 ont été levés en expérimentation no-code. 3.7/3.8 restent à dérisquer (modéré). Section 5 de specs-techniques-v3 à réécrire en début de Phase 3 (un bandeau de décision est ajouté pour signaler le repositionnement). |
| 2026-04-30 | **Phase 3.3 — couche LLM générique livrée** | `src/llm/` : abstraction `LLMProvider` (ABC) + `FakeLLMProvider` (déterministe pour tests, traçant les appels) + `ClaudeAPIProvider` (SDK officiel `anthropic`, system-prompt extrait, multi text-blocks concat). Helper `extract_json_block` cascade fence → balanced-braces → fallback (gère prose autour, accolades dans strings, JSON invalide). Décorrelation totale code applicatif ↔ SDK Claude : tests sans clé API ni dépendance réseau. 14 tests. | Foundation pour les 5 agents 3.4-3.8. Permet de basculer Mistral / GPT en changeant 1 ligne d'instanciation. Évite le lock-in Anthropic-MCP. |
| 2026-04-30 | **Phases 3.4-3.8 — 5 agents méca livrés** | `src/agents/` (engine générique) : `Agent` ABC single-shot prompt → JSON Pydantic. `src/verticals/mech_workshop/agents/` (5 agents) : (3.4) `QuestionnaireAgent` + `WorkshopSpec` ; (3.5) `CSVExtractionAgent` + `CSVExtractionOutput` (réutilise `prompt_v3.md` validé 45/45) ; (3.6) `SoftConstraintsAgent` + 10 catégories canoniques (réutilise `prompt_v1_soft.md` validé 75/75) ; (3.7) `ExplanationAgent` + `ExplanationOutput` (placement / infeasibility) ; (3.8) `ConversationalEditAgent` + `EditPlan` (7 actions canoniques + clarification path). Tous les schémas Pydantic ont `extra="forbid"`, `frozen=True`, validators métier. 19 tests avec FakeLLMProvider, 0 appel API. | 3.5 et 3.6 stabilisés (prompts validés empiriquement en expérimentation). 3.4, 3.7, 3.8 en 🔵 en test : scaffolding livré, validation manuelle avec design partners reste à faire (10/30/10 cas types selon l'étape). |
| 2026-04-30 | **CLI no-code `scripts/agent_io.py` + premier trial 3.6** | Ajout d'un workflow no-code en 3 étapes (`prompt` → claude.ai → `validate`) pour itérer les agents sans clé API. Premier trial réel de 3.6 sur 10 phrases méca (différentes de l'expérimentation Phase 2) : **10/10 correctement classifiées**, dont la phrase piège « Jamais de rectif sans certif » correctement basculée en `unrecognized` avec suggestion architecturalement cohérente (pointe sur `qualified_operator_constraint` qui existe en code). Voir `experiments/agents-trial/results.md`. | Confirme que 3.6 tient hors expérimentation. Reproductibilité du résultat 75/75 sur des inputs nouveaux. Reste à valider 3.4 / 3.7 / 3.8 par trials manuels avant de considérer Phase 3 entièrement stabilisée. |
| 2026-04-30 | **Trials 3.4 / 3.7 / 3.8 — un mini-bug attrapé sur 3.7** | Trial 3.4 sur 1 input PME aero : ✓ excellent (estimations milieu de fourchette + notes explicatives, mappings enums respectés). Trial 3.7 sur `placement` : ✗ **Claude a omis le champ `kind`** → prompt v1 durci (règle 5 renforcée, mention en tête de Tâche). Trial 3.7 sur `infeasibility` : ✓ excellent (3 reasons, 3 actions actionnables, 0 jargon solveur). Trial 3.8 sur cas piège conditionnel : ✓ valide, **comportement pro-actif observé** (Claude tranche les conditions floues `si trop chargé` plutôt que demander clarification ; comportement défendable mais à surveiller). | Le workflow no-code attrape de vrais bugs de prompt avant qu'ils n'arrivent en prod. Le pattern « scaffolding + Pydantic strict + validators métier » a fait son boulot : le bug 3.7 (`kind` manquant) aurait été silencieux sans la `ValidationError`. Reste à re-tester `placement` 3.7 + reproduire 3.4/3.8 sur 4-5 inputs variés avant ✅. |
| 2026-04-30 | **Phase 3 ✅ entièrement stabilisée** | 5 trials supplémentaires : 3.7 placement re-test (prompt v1.1) ✓, 3.4 méca auto IATF 16949 ✓, 3.4 méca médical ISO 13485 ✓, 3.4 polyvalent sans certif ✓ (avec note honnête de Claude sur la limite enum `tour_conventionnel`), 3.8 condition non tranchable 51%/49% ✓ (`needs_clarification=true` cette fois — comportement discriminant confirmé). **Bilan total** : 13 trials réels sur les 5 agents, 12 ✓ + 1 fail attrapé par Pydantic et corrigé. **Phase 3 close**, 6/8 étapes ✅ (3.1 + 3.2 abandonnées MCP, 3.3-3.8 stabilisées). | Tous les agents tiennent en conditions réelles. Limites d'enum identifiées (tour_conventionnel, salle_blanche, plastique/PEEK) à traiter post-pilotes — ne bloquent pas la Phase 4 backend. La doctrine "scaffolding générique + Pydantic strict + validation no-code" est validée comme méthodologie de dev d'agents LLM. Voir `experiments/agents-trial/results.md` pour les détails. |
| 2026-04-30 | **Phase 1.6 démarrée — soft constraints côté solver** | Engine livré : `src/core/soft_constraints.py` (`SoftPenaltyVar` + `WeightedObjectivePattern` qui combine `minimize(makespan + Σ w_i × penalty_i)`). Extension `Job` avec `deadline: int \| None` et `client: str \| None` (champs optionnels backward-compatible). Verticale : 3 translators couvrant 3 idiomes CP-SAT distincts (PROPORTIONNEL pour tardiness_per_job, BOOLÉEN reified pour avoid_machine_during_period, PROPORTIONNEL aggregat pour encourage_early_completion). Le solver accepte un `soft_penalty_builder` injectable, vertical-agnostic. Note critique : avec soft penalties, `solver.objective_value` ≠ makespan ; on lit explicitement `solver.value(makespan_var)` pour récupérer le makespan brut. Cohabitation testée : **5 hard (NoOverlap + Precedence + Setup + Calendar + Operator + SharedResource) + 3 soft simultanés** sur instance non-triviale → ✓ passe. 291 tests passants (+12), 0 régression, ruff clean. | 1.6 reste 🟡 : il reste 2 idiomes à couvrir (COUNT pour grouping/limit_setups, CHOICE pour prefer_machine_over_other) et donc 4 translators sur 7 catégories cliniques restantes (5/10 catégories du module 3.6 traitées au total avec celles déjà livrées). Le pattern engine ↔ vertical tient pour la 5e fois consécutive. |
| 2026-04-30 | **Phase 1.6 ✅ stabilisée — critère de sortie 5 soft + 5 hard atteint** | Ajout de 3 translators méca couvrant 2 nouveaux idiomes : `limit_ops_per_day_on_machine` (idiome COUNT bucketé, encodage `in_day = ge_low ∧ lt_high` reified par op et par jour, excès = max(0, count - max_per_day)) ; `prefer_grouping_by_family` (idiome SPREAD min/max, sum des `(max_end - min_start)` par (machine_id, family_id) avec ≥ 2 ops) ; `prefer_grouping_by_client` (même idiome, group par (machine_id, client), filtrable par `client_reference`). 6 translators au total couvrant 4 idiomes CP-SAT distincts. Dispatcher mis à jour avec les 3 nouvelles catégories + paramètre `day_offsets`. Test `test_solver_5_hard_5_soft_cohabitate` : NoOverlap + Precedence + Setup + Calendar + Operator + SharedResource + 5 soft simultanés → ✓ passe. 298 tests passants (+7), 0 régression, ruff clean. | **Phase 1.6 close**. Catégories non livrées documentées et déférées V2 : `prefer_machine_over_other` (nécessite refactor modèle pour rendre `Operation.machine_id` décisionnel), `prefer_operation_in_shift` (encodage cyclique), `operator_avoidance/preference` (exposition des `present[i][k]` du QualifiedOperatorPattern). Restantes Phase 1 : 1.1.opt (setup pattern bottleneck — toujours prioritaire avant scale), 1.2-1.5 + 1.7-1.8. |
| 2026-04-30 | **Phase 1.2 ✅ — objectif composite via priorités nommées** | Engine livré : `src/core/objectives.py` (ABC). 4 objectifs universels (makespan, tardiness, stability, early_completion), chacun typé via `ObjectivePriority` (DISABLED/LOW/MEDIUM/HIGH/CRITICAL) mappé sur une échelle géométrique 0/1/5/25/125 (chaque cran domine clairement le précédent). `CompositeObjectiveSpec` Pydantic strict avec defaults raisonnables. `build_composite_soft_penalties()` produit un `SoftPenaltyBuilder` configuré, composable avec un `vertical_extras` pour étendre avec les soft NL-driven de la verticale. Helpers engine ajoutés à `soft_constraints.py` : `aggregate_tardiness_var`, `aggregate_completion_var`, `schedule_stability_var` (réplanification = `Σ \|new_start - old_start\|` via `add_abs_equality`). Solver accepte `makespan_weight` paramétrable. Tests : 18 (316 total) dont **replanification CRITICAL stability** qui force le re-solve à retomber exactement sur le reference schedule (déviation 0). | Critère "solveur accepte priorités relatives utilisateur" atteint. Le pattern engine ↔ vertical tient pour la 6e fois consécutive. Le `vertical_extras` callable permet de combiner objectifs universels + soft constraints méca NL-driven dans la même fonction objectif. À calibrer en Phase 1.3 (calibration dynamique des poids selon caractéristiques d'instance). |
| 2026-04-30 | **Phase 1.3 ✅ — calibration dynamique des poids** | Engine : extension `SoftPenaltyVar.expected_max: int \| None` (annotation backward-compat). `calibrate_penalty_weights(penalties, target=1000)` rescale `new_weight = max(1, round(weight × target / expected_max))`. `build_composite_soft_penalties(calibrate=True)` par défaut. **Verticalité respectée** : engine fournit le mécanisme + calcule `expected_max` pour les 4 objectifs universels (depuis horizon × n_jobs) ; les 6 translators méca annotent chacun leur `expected_max` selon leur idiome (n_ops_on_machine pour avoid_period, horizon × n_groupes pour spreads, n_ops × n_jours pour limit_setups, etc.). **Test discriminant** `test_calibration_prevents_dominance_of_large_scale_penalty` : 2 pénalités de même priorité MEDIUM mais d'échelles 100× différentes — sans calibration, la grande écrase la petite ; avec calibration, la petite reçoit un poids 100× plus grand pour compenser → ratio des contributions ≈ 1. 16 tests, 332 total. Tests 1.2 ajustés (`calibrate=False` explicite pour vérifier les poids bruts). | Le pattern engine ↔ vertical tient pour la **7e fois** consécutive. Backward-compatible : `expected_max=None` (default) = pas de calibration appliquée → comportement Phase 1.2. La doctrine "engine fournit le mécanisme, vertical fournit la calibration" tient même pour la calibration elle-même : c'est récursif et ça marche. |
| 2026-04-30 | **Phase 1.4 ✅ — replanification incrémentale (freeze + hint)** | Engine : nouveau module `src/core/replanification.py` avec `FreezeSpec` / `SolutionHintSpec` (Pydantic frozen). Helpers `apply_freeze` (contrainte hard `start_var == start_value`), `apply_solution_hint` (`model.add_hint(var, value)`), et surtout `derive_freeze_and_hint_from_previous(previous_result, *, now)` qui automatise le split en politique générique : ops avec `start ≤ now` → freeze (déjà démarrées, intouchables), ops futures → hint (warm start). Solver accepte `freeze=` et `solution_hint=`. **Verticalité respectée** : la dérivation et les helpers opèrent uniquement sur `(job_id, seq_idx)` — clés universelles, aucune catégorie métier. La verticale peut spécialiser via custom logic (ex: "freeze setups en cours en méca") mais V1 n'en a pas besoin. **Caveat documenté** par test explicite : un freeze peut pousser au-delà du horizon naïf et provoquer INFEASIBLE (V2 : faire `_compute_horizon` prendre en compte les freezes). **Critère de sortie** validé via test slow sur 8×8 (avec garde-fou pour les instances trop petites où l'overhead du hint > bénéfice). 16 tests, 348 total. | Le pattern engine ↔ vertical tient pour la **8e fois**. Phase 1 avance bien : 1.1 + 1.2 + 1.3 + 1.4 + 1.6 stabilisées. Restantes : 1.1.opt (toujours prioritaire), 1.5 (stabilité Tier), 1.7 (clustering familles), 1.8 (MIS approximé). |
| 2026-04-30 | **Phase 1.5 ✅ — stabilité pondérée par criticité Tier 1/2/3** | Engine : ajout `Job.criticality: int \| None` (1 = critique, 3 = standard, convention tier-supplier inversée). Helper `tier_weighted_stability_var(weight_per_tier, default_weight)` qui pondère `\|new_start - old_start\|` par le tier de chaque job ; jobs sans criticality → `default_weight`. Extension `build_composite_soft_penalties(stability_tier_weights=...)` : bascule automatique sur la version tier-pondérée si fourni (label `composite_stability_tier_weighted`), sinon version uniforme (label `composite_stability` de Phase 1.4) — backward-compatible. **Verticalité respectée — 9e fois consécutive** : engine fournit le mécanisme universel (la pondération par tier est universelle, pas spécifique méca) ; verticale fournit la calibration via `MECH_TIER_WEIGHTS = {1: 10, 2: 3, 3: 1}` dans nouveau module `replanification_config.py`. Une autre verticale (santé : urgent vs programmé ; éducation : examen vs cours) fournirait sa propre échelle. **Critère de sortie strict** validé : `test_critere_de_sortie_tier_1_cout_10x_tier_3` montre ratio = 10 sur valeurs effectives. **Test discriminant** `test_tier_weighted_solver_prefers_to_move_tier_3` : sur instance forcée à déplacer 1 op, le solveur garde le T1 et bouge le T3 (comportement métier attendu). 11 tests, 359 total. | Phase 1 maintenant : 1.1 + 1.2 + 1.3 + 1.4 + **1.5** + 1.6 stabilisées (6/9). Restantes : 1.1.opt, 1.7, 1.8. La replanification est mature : freeze (1.4) + hint (1.4) + stability tier-pondérée (1.5) + calibration (1.3) + composite priorités (1.2) — tout l'outillage est en place pour des replans pilotes design partners. |
| 2026-04-30 | **Phase 1.7 ✅ — clustering automatique des familles** | Engine : nouveau module `src/core/clustering.py` avec `agglomerative_cluster(items, distance_fn, target_n_clusters, merge_distance_threshold)` — algorithme greedy average-linkage. Vertical-agnostic : la verticale fournit la `distance_fn`. Threshold optionnel pour éviter de fusionner des items dissemblables juste pour atteindre `target_min`. Verticale `mech_workshop/clustering.py` : `order_distance(o1, o2)` combine Jaccard sur les ops (50 %) + match matériau (30 %) + Jaccard sur les machines (20 %), justification pondération documentée. **Verticalité respectée — 10e fois consécutive**. **Critère de sortie atteint** : 100 OF synthétiques répartis en 8 archétypes → 5-15 familles avec target=(5, 15) ; chaque archétype 100 % dans son cluster avec target=(5, 8). 13 tests, 372 total. | Phase 1 maintenant : 7/9 stabilisées (1.1, 1.2, 1.3, 1.4, 1.5, 1.6, **1.7**). Restantes : 1.1.opt et 1.8 (extraction MIS). |

---

## Journal des retours en arrière

> Si une phase ultérieure révèle un manque dans une phase antérieure, on le trace ici.

| Date | Phase impactée | Étape | Raison | Action |
|------|---------------|-------|--------|--------|
| 2026-04-30 | Phase 3 | 3.1 + 3.2 | Le serveur MCP était une cosmétique technique sans valeur produit pour cette SaaS B2B (backend orchestre lui-même les appels Claude API). Surcoût d'un protocole + lock-in Anthropic + dispersion d'effort vs. la vraie valeur (agents 3.4 → 3.8). | 3.1 et 3.2 marquées ❌ abandonnées. 3.3 réduit en scope (juste un client Claude API + tool use natif + interface fine pour fallback). Bandeau de décision ajouté en tête de specs-techniques-v3 §5. Réécriture profonde de §5 différée au début de Phase 3 effective. |

---

*Document créé le 2026-04-29.*
