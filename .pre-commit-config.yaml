# Configuration pre-commit
# Activation : `uv pip install pre-commit && pre-commit install`
# Exécution manuelle : `pre-commit run --all-files`

repos:
  # Hooks génériques
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v5.0.0
    hooks:
      - id: trailing-whitespace
        exclude: \.md$
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-toml
      - id: check-json
      - id: check-added-large-files
        args: ['--maxkb=1024']
      - id: check-merge-conflict
      - id: check-case-conflict
      - id: mixed-line-ending
        args: ['--fix=lf']
      - id: detect-private-key

  # Ruff — lint + format Python (rapide, remplace black + isort + flake8)
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.7.4
    hooks:
      - id: ruff
        args: [--fix]
      - id: ruff-format

# Hooks à activer quand les composants existent :
#
#  - repo: https://github.com/pre-commit/mirrors-mypy
#    rev: v1.13.0
#    hooks:
#      - id: mypy
#        args: [--strict]
#        files: ^poc-scheduler/src/
#
#  - repo: https://github.com/pre-commit/mirrors-prettier
#    rev: v4.0.0-alpha.8
#    hooks:
#      - id: prettier
#        files: \.(ts|tsx|js|jsx|json|css|scss|md)$
